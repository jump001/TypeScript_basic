const point: [number, number] = [23, 44]; //x=23 y=44
//โดยธรรมชาติของอาเรจะเข้าถึงตำแหน่งไหนก็ได้ จึงต้องจำกัดช่องไว้
// แต่เรา สามารถ ส่ง point.push(30) เพิ่มเข้าได้ แต่ก็จะเข้าถึงได้แค่2ตำแหน่งคือ 23และ44 ป้องกันการอ่านค่าเท่านั้น
//ดังนั้นถ้าช่องเกิน ให้ใช้วิธีกำหนด label
/*
const point: [x:number,y:number]=[23,44]; ทำให้อ่านง่ายขึ้น
*/
